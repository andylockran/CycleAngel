This repo is based on django/wagtail.

It uses virtualenv - so start by running:

  virtualenv venv;
  source ./venv/bin/activate;

at the end of the working on the project, deactivat the venv by running:
  
  deactivate

